/* 
 * Loading - Styles
 */

const styles = {
    container: userSX => ({
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
        width: "100%",
        ...userSX
    })
};

export default styles;